{
  "name": "centinel",
  "version": "0.0.1",
  "scripts": {
    "do-it": "npm run cleanup && npm run build && node build/index.js",
    "build": "mkdir artifacts && tsc",
    "cleanup": "rimraf ./build ./artifacts"
  },
  "devDependencies": {
    "@types/node": "^17.0.12",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@types/lodash": "^4.14.178",
    "dayjs": "^1.10.7",
    "lodash": "^4.17.21"
  }
}
